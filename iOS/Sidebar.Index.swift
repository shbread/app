import Foundation

extension Sidebar {
    enum Index: Int {
        case
        settings = -1,
        capacity = -2,
        markdown = -3,
        privacy = -4,
        terms = -5,
        full = -6
    }
}
